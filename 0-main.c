#include "stdio.h"
#include "stdlib.h"
#include "unistd.h"
#include "main.h"
/**
 * main - runs it all
 * @argc: number of arguments
 * @argv: an array of pointers to strings
 *
 * Description: this is where the program starts and ends
 * Return: always 0.
 */
int main(int argc, char *argv[])
{
	return (0);
}
